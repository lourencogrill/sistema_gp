// Placeholder para a página de histórico de avaliações
export default function EvaluationHistoryPage({ params }: { params: { id: string } }) {
  return <div>Histórico da Avaliação: {params.id}</div>;
} 